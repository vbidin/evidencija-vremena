<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/calendar_css")
    @Scripts.Render("~/Content/duallist_css")
    @Scripts.Render("~/bundles/modernizr")
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/moment")
    @Scripts.Render("~/bundles/chart")
    @Scripts.Render("~/bundles/calendar")
    @Scripts.Render("~/bundles/duallist")
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Predmeti", "Index", "Predmet")</li>
                    <li>@Html.ActionLink("Evidencija", "Index", "Evidencija")</li>
                    <li>@Html.ActionLink("Statistika", "Index", "Statistika")</li>
                </ul>
                <ul class="nav navbar-right navbar-nav">
                    @if (Request.IsAuthenticated)
                    {
                        <li class="navbar-right">@Html.ActionLink("Odjava", "Odjava", "Autorizacija")</li>
                        <li class="navbar-right">@Html.ActionLink(User.Identity.Name, "Index", "Profil")</li>}
                    else
                    {
                        <li class="navbar-right">@Html.ActionLink("Registracija", "Registracija", "Autorizacija")</li>
                        <li class="navbar-right">@Html.ActionLink("Prijava", "Prijava", "Autorizacija")</li>}
                </ul>

            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
    </div>
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
